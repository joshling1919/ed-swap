const defaultState = {
  currentUser: {},
};

const SessionReducer = (state = defaultState, action) => {
  // switch (action.type) {
  //   case expression:
  //
  //   default:
  //     return state;
  // }
  return state;
};

export default SessionReducer;
